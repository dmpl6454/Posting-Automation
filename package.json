{
  "name": "posting-automation",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "db:migrate": "pnpm --filter @postautomation/db db:migrate",
    "db:push": "pnpm --filter @postautomation/db db:push",
    "db:seed": "pnpm --filter @postautomation/db db:seed",
    "db:studio": "pnpm --filter @postautomation/db db:studio",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "turbo": "^2.8.17",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "patchedDependencies": {
      "@auth/core@0.41.0": "patches/@auth__core@0.41.0.patch"
    }
  }
}
