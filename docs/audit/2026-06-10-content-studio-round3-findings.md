I now have the complete content for all 6 finished investigators plus their 23 verifiers, and confirmed the 7th issue (prod-logs) is truncated. Here is the full extraction.

---

# Workflow Extraction — wf_b133151f-32b

**Coverage:** 6 of 7 investigators completed and emitted a `StructuredOutput`. The 7th (`prod-logs`, transcript `agent-a5cc36a067b4f6e1d`) is **TRUNCATED / INCOMPLETE** — it was the production-log ground-truthing agent (SSH into `postautomation-web-1`/`worker-1`), but its transcript ends mid-investigation (last assistant text: *"nginx reflects `$http_origin` while MinIO also emits its own ACAO → duplicate header → Chrome CORS rejection. Now inspecting the actual creatives generated today."*) with no StructuredOutput. No findings are available for it. All 23 verifiers completed and each matched 1:1 to a root cause. Only **one root cause was REFUTED**: static-blank RC1. Two root causes (aesthetic RC6, static-blank RC6) had no verifier assigned.

---

## ISSUE: seedance-text — Seedance AI video "contains a lot of text"
*(agent-a01a39dc380efc73c)*
**NOTES:** buildSeedancePrompt (seedance.provider.ts:254-278) contains NO anti-text instruction — it demands bold text overlays, a zooming title, `Bold white text "${point}"` per key point, a CTA with typography animation, and `Text: SUPER BOLD...`. `git show ce602f0` confirms Phase 2b changed ONLY the final people line; every text directive survived. The worker burns `[title, ...scenes.slice(0,4)]` = up to 5 permanent caption lines (fontsize 44, no `enable=` timing), so the two text layers stack: garbled model-rendered text PLUS 5 clean burned lines of the same script. The worker's comment claiming "we generate with NO text in the model output" is currently false. Fix needs BOTH prompt rewrite (RC1) and burn reduction (RC2). Read-only; no files modified.

### Root cause 1 [confidence: high] [verdict: confirmed]
**CLAIM:** buildSeedancePrompt affirmatively commands the Seedance model to render the title, every key point, and a CTA as on-screen text ('Bold white text', 'SUPER BOLD, extra large') and contains zero 'no on-screen text/captions/subtitles/watermarks' instruction — so the model paints (and garbles) the entire script on screen.
**EVIDENCE:**
- `packages/ai/src/providers/seedance.provider.ts:261-277` — scenes mapper: `.map((point, i) => \`Scene ${i + 2}: Bold white text "${point}" centered on screen over dramatic cinematic B-roll.\`)`
- `packages/ai/src/providers/seedance.provider.ts:277 (git show ce602f0)` — Phase 2b diff changed ONLY: `-Do NOT show real people's faces...` → `+You may show generic, anonymous people... Do NOT depict any specific, real, named public figure...`
- `apps/worker/src/workers/repurpose-video.worker.ts:67-71` — comment: "Seedance garbles any in-video text, so we generate with NO text in the model output and overlay the readable title + scenes here" (contradicts the prompt)
- `apps/worker/src/workers/repurpose-video.worker.ts:250-255` — worker feeds `title: sd.title.slice(0,60)`, `keyPoints: sd.scenes` into the prompt
- `packages/api/src/routers/repurpose.router.ts:1080-1088, 1106-1109, 1129-1136` — scenes = 4-6 LLM key points (max 15 words), fallback = up to 5 body sentences sliced to 80 chars
**PROPOSED FIX:** Rewrite buildSeedancePrompt (seedance.provider.ts:254-278) to make the model generate VISUALS ONLY: (1) change scenes mapper (263-265) from `Bold white text "${point}"` to e.g. `Scene ${i + 2}: Cinematic B-roll visually depicting: ${point}.`; (2) change Scene 1 (271) to a textless opening shot (drop the zooming title text — describe the subject visually); (3) delete the Final-scene CTA typography line (273) and the `Text: SUPER BOLD...` directive (276); (4) drop 'with bold text overlays' from the opening sentence (267); (5) append a hard negative clause after the people line (277): `Do NOT render any on-screen text, words, letters, numbers, captions, subtitles, lower-thirds, chyrons, titles, logos, watermarks, or typography of any kind anywhere in the video.` This makes the prompt match the worker's stated invariant. `brandName` opt becomes unused (worker doesn't pass it anyway).
**VERIFIER (refuted=False):** Verified verbatim; every element holds. Five affirmative text directives confirmed (lines 264/267/271/273/276); only "Do NOT" is the line-277 likeness clause. git show ce602f0 confirms one-line people-policy change only. Worker comment 67-72 directly contradicted. Worker passes title/keyPoints/visualStyle/musicMood, NOT brandName → CTA defaults to "Follow for More". buildSeedancePrompt has exactly two callers (this worker + seedance-prompt.test.ts, which only asserts the people clause/9:16/title) — proposed rewrite is test-compatible if the title remains.

### Root cause 2 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The ffmpeg drawtext burn overlays up to FIVE permanent caption lines — the title plus the first four scene sentences, each up to 80 chars at fontsize 44 in black boxes stacked 70px apart at the bottom — with no enable= timing, so all five lines cover roughly the bottom third of the 9:16 frame for the entire 2-12s clip, on top of the model's own rendered text.
**EVIDENCE:**
- `apps/worker/src/workers/repurpose-video.worker.ts:271` — `videoBuf = burnCaptionsOnVideo(raw, [sd.title, ...sd.scenes.slice(0, 4)]);`
- `apps/worker/src/workers/repurpose-video.worker.ts:86-94` — one drawtext per line, `lineHeight = 70`, `y = h-text_h-${60 + (cleanLines.length-1-i)*lineHeight}`, no `enable=`
- `apps/worker/src/lib/repurpose-video.ts:82-92` — `escapeDrawText` (escaping only, no shorten/time)
**PROPOSED FIX:** Reduce + time-slice the burn. Option A (minimal): `burnCaptionsOnVideo(raw, [sd.title])` — one clean lower-third line. Option B (recommended): keep title persistent and rotate ONE scene caption at a time using drawtext's `enable='between(t,START,END)'`: extend burnCaptionsOnVideo (73-132) to accept `Array<{text; start?; end?}>` (or durations param + the clip's `sd.duration` at line 260), build title filter without `enable=`, give each of `sd.scenes.slice(0,4)` an equal window `enable='between(t,i*seg,(i+1)*seg)'` (seg = duration/sceneCount) at a fixed y above the title — at most 2 lines visible at once. The colon in `between(t,a,b)` must NOT be escaped. Also update the now-false invariant comment at 67-71 once RC1 lands.
**VERIFIER (refuted=False):** Both files read in full; line 271 = 5 lines, lines 86-94 match (no `enable=` anywhere). Duration 2-12s confirmed (seedance.provider.ts:88, default 5). **CORRECTION:** Claim accurate, two precision notes: (1) at the actual default output resolution (720p → 720×1280, since worker never passes `resolution`), the 5-line stack ≈ 400px ≈ 31% of frame height — "roughly bottom third" holds, and since the model text is CENTERED, the burn overlaps the lower portion rather than covering all of it; (2) the Option-B `enable='between(t,...)'` must keep the filtergraph-level single quotes — its commas are safe because of that filtergraph quoting, not merely the unquoted execFileSync array element.

---

## ISSUE: progress — Activity Log spins forever after successful Seedance video
*(agent-a3ab3dc11ec725e26)*
**NOTES:** Worker publish order for seedance_video: "Generating AI video (Seedance)" running (239), repeated every 5s with detail `${elapsedSeconds}s — ${status}` (262-264), "Adding captions" running (269), "Uploading video" running w/ MB detail (278), then `video_ready` done (294) and `__finished__` done via finishProgress (295). NONE of the three user-visible step names ever gets status="done". Earlier router steps show checkmarks because the router's `progress()` helper does start("running")/finish("done") pairs and the client dedupes by step-name. The client's video_ready branch `return`s (RepurposeTab.tsx:195) before setProgressSteps and closeVideoStream() closes the EventSource — so the worker's trailing `__finished__` is never received. Recommend BOTH worker-side "done" publishes AND client-side finalize-on-terminal-event, plus the router publish reorder. Branch fix/content-studio-phase1.

### Root cause 1 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The repurpose-video worker publishes its three Seedance steps ("Generating AI video (Seedance)", "Adding captions", "Uploading video") ONLY with status "running" and never re-publishes any of them with status "done" — the only "done" publishes are the synthetic terminal steps `video_ready` and `__finished__` (via finishProgress), so each named step's last-known status in the SSE stream is permanently "running".
**EVIDENCE:**
- `apps/worker/src/workers/repurpose-video.worker.ts:238-239, 262-264` — `await pushProgress(scoped, "Generating AI video (Seedance)", "running")` and the onProgress `void pushProgress(... "running", \`${elapsedSeconds}s — ${status}\`)`
- `apps/worker/src/workers/repurpose-video.worker.ts:269, 278` — `pushProgress(scoped, "Adding captions", "running")` / `pushProgress(scoped, "Uploading video", "running", ...MB)`
- `apps/worker/src/workers/repurpose-video.worker.ts:294-295` — `pushProgress(scoped, "video_ready", "done", ...)` then `finishProgress(scoped, "done")`
- `apps/worker/src/workers/repurpose-video.worker.ts:151-152` — reel branch "Stitching reel video" "running"
**PROPOSED FIX:** In repurpose-video.worker.ts mirror the router's start/finish pattern: after `generateSeedanceVideo` resolves (between 265 and 269) push `("Generating AI video (Seedance)", "done", \`${seed.durationSeconds}s video\`)`; after `burnCaptionsOnVideo` returns (after 271) push `("Adding captions", "done")`; after `uploadVideoToS3` resolves (after 280) push `("Uploading video", "done", ...MB)`. Same for the reel branch's "Stitching reel video" after 236. Client dedupes by step name (RepurposeTab.tsx:208-217), so each re-publish flips spinner → checkmark.
**VERIFIER (refuted=False):** Every cited line matches. Only "done" publishes are video_ready (294) + finishProgress (295, = `pushProgress(jobId,"__finished__",status)` per progress.ts:89-91). Error path (298-307) only adds video_error/__finished__ "error". Fix anchors accurate (generateSeedanceVideo @265, burn @271, upload @280, reel @236). **CORRECTION:** (1) "Uploading video" (278) is in the shared post-branch path so it runs for BOTH reel and seedance — the reel branch has two stuck steps. (2) Consequence is stronger: client's video_ready handler returns before setProgressSteps and closes the SSE stream, so spinner rows are never updated after success; on failure only video_error/__finished__ ("error") fire, leaving named steps "running".

### Root cause 2 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The client never finalizes outstanding in-progress steps when the run ends: the `video_ready` handler in RepurposeTab.tsx returns early BEFORE the `setProgressSteps` updater (so it neither records the terminal event nor touches prior rows), and the `__finished__` handler just closes the EventSource — no code maps existing status==="running" rows to "done", and the render shows a spinning Loader2 for any row whose last status is "running" forever.
**EVIDENCE:**
- `apps/web/components/content-agent/RepurposeTab.tsx:164-169` — `if (step.step === "__finished__") { es.close(); eventSourceRef.current = null; return; }`
- `apps/web/components/content-agent/RepurposeTab.tsx:174-196` — `if (ready) { setResults(...); setVideoGenerating(false); toast(...); closeVideoStream(); ... }`
- `apps/web/components/content-agent/RepurposeTab.tsx:208-217` — dedupe upsert by `findIndex((s) => s.step === step.step)`
- `apps/web/components/content-agent/RepurposeTab.tsx:1125-1128` — `{s.status === "running" && <Loader2 ... animate-spin />}` etc.
**PROPOSED FIX:** Belt-and-braces client fix in startProgress (162-218): inside `if (ready)` (before `closeVideoStream()` at 194) and inside the `__finished__` branch (165, when status "done"), call `setProgressSteps((prev) => prev.map((s) => s.status === "running" ? { ...s, status: "done" as const } : s))`; in the `isVideoErrorEvent` branch (197) map outstanding "running" → "error". Makes the log self-heal even if the worker dies. The finalize must live in the video_ready branch (not only `__finished__`) because the client closes the stream on video_ready (194), so `__finished__` is never seen.
**VERIFIER (refuted=False):** All confirmed. video_ready branch ends with closeVideoStream() @194 + return @195, both before the upsert @208-217. `__finished__` only does es.close/null/return. No code maps "running"→"done". Render @1125-1128 spins forever. Worker closes happen before __finished__ is seen on video path. **CORRECTION:** (a) cited "startProgress (162-218)" is actually the es.onmessage handler body; startProgress spans 153-227 (all referenced line numbers exact). (b) Bug is worse than "even if worker dies": the worker NEVER re-pushes intermediate steps as "done" (only "running" at 152/239/263/269/278), so frozen spinners happen on EVERY successful async reel/seedance run. Fix placement correct (video_ready branch before closeVideoStream@194); a __finished__-branch finalize additionally self-heals the synchronous path.

### Root cause 3 [confidence: high] [verdict: CORRECTED]
**CLAIM:** Cosmetic "0 scenes" vs "5 scenes": the router publishes the extraction step's "done — N scenes" detail at repurpose.router.ts:1101 using keyPoints.length BEFORE the sentence-split fallback (lines 1106-1109) fills keyPoints — when the LLM reply contains no JSON array, `arrMatch` is null (no throw), keyPoints stays [] so the step says "0 scenes", then the fallback slices 5 sentences and the "Queued AI video for generation" step at line 1119 reports "5 scenes".
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:1090-1109` — `const arrMatch = cleaned.match(/\[[\s\S]*\]/); if (arrMatch) keyPoints = JSON.parse(arrMatch[0]);`
- `packages/api/src/routers/repurpose.router.ts:1119` — `progress("Queued AI video for generation", "done", \`${keyPoints.length} scenes\`)`
- `packages/api/src/routers/repurpose.router.ts:916-929` — same pattern in the veo3 ai_video branch
**PROPOSED FIX:** Move the success publish for "Extracting key points for video scenes" to AFTER the `if (keyPoints.length === 0)` fallback block (relocate line 1101's publish to just after 1109), optionally detail `"5 scenes (from article sentences)"` when the fallback ran. Because the client dedupes by step name, re-publishing after the fallback also works. Apply the same reorder to the veo3 `ai_video` branch (916-929).
**VERIFIER (refuted=False):** Confirmed exactly. Seedance branch arrMatch null → keyPoints [] → "0 scenes" @1101, fallback `sentences.slice(0,5)` @1106-1109, line 1119 → "5 scenes". Veo3 branch same pattern (927 done-publish, 929 error, fallback 933-936, line 951 a console.log). Dedupe assumption verified. **CORRECTION:** (1) fallback yields up to 5 (`sentences.slice(0,5)` filtered for length>20); if body has zero qualifying sentences both say "0 scenes" (no mismatch). (2) In veo3 branch line 951 is a server-side console.log not a progress event — the user-visible contradiction exists only in seedance_video (veo3 just shows a misleading "0 scenes" and is currently UI-disabled). A re-publish after the fallback also reconciles the catch/error path, whereas a pure relocation would not fire on error.

### Root cause 4 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The "160s" in "Generating AI video (Seedance) — 160s — IN_PROGRESS" is the ELAPSED POLL TIME of the fal.ai queue poll (poll iteration count x 5s), not the video duration: seedance.provider.ts computes `elapsed = Math.round(((i + 1) * POLL_INTERVAL) / 1000)` (POLL_INTERVAL=5000) and passes it to onProgress as elapsedSeconds together with fal's queue status; the worker formats it as `${elapsedSeconds}s — ${status}` — it freezes at the last pre-COMPLETED poll value (160s = 32nd poll) because the step is never re-published with a final status (root cause 1).
**EVIDENCE:**
- `packages/ai/src/providers/seedance.provider.ts:151-152, 192-196` — `MAX_POLLS = 90`, `POLL_INTERVAL = 5000`, `const elapsed = Math.round(((i + 1) * POLL_INTERVAL) / 1000)`
- `apps/worker/src/workers/repurpose-video.worker.ts:262-264` — `onProgress: ({ elapsedSeconds, status }) => { void pushProgress(scoped, "Generating AI video (Seedance)", "running", \`${elapsedSeconds}s — ${status}\`); }`
- `apps/web/components/content-agent/RepurposeTab.tsx:1130-1135` — renders `{s.step} — {s.detail}`
**PROPOSED FIX:** No standalone fix needed — fixing RC1 resolves the stale detail. Optionally, when publishing the post-generation "done" (worker ~266) set a human detail like `generated in ${elapsed}s` or `${seed.durationSeconds}s clip`. If the raw fal status is confusing, map IN_QUEUE/IN_PROGRESS → "queued"/"rendering" in the worker's onProgress (repurpose-video.worker.ts:263).
**VERIFIER (refuted=False):** Confirmed. COMPLETED branch breaks BEFORE onProgress fires → last published event is always the final pre-COMPLETED poll. Math: 160 = 32×5s. Proposed fix coherent. **CORRECTION:** Only trivial line-number imprecision: MAX_POLLS/POLL_INTERVAL at seedance.provider.ts:152-153 (not 151-152); elapsed/onProgress block at 193-197 (not 192-196). Semantics, formatting, freeze mechanism, and UI all match exactly.

---

## ISSUE: aesthetic — aestheticRefUrl is a silent no-op + degrades on social PAGE URLs
*(agent-a48411d019450584d)*
**NOTES:** A bad/HTML reference CANNOT produce "no image at all" on static/carousel — renderStaticCreative catches AI-bg failure (repurpose.router.ts:177-189) and the Puppeteer template still renders with a stock/theme bg; carousel slides have per-slide isolation. Most plausible mechanism for "blank carousel images": the IG URL was pasted into the imageContext textarea, polluting every slide's bgPrompt with `Style notes: https://...` (router:848 static, :1263-1266 carousel), tripping gpt-image-1 moderation/URL-text renders → stock/themed bg that reads as "blank". Confirmed: in prod the billing-403 message matches none of isSafetyBlock's regex, so every call takes the non-safety branch straight to generateImageDallE (prompt+size only) — references have ZERO effect in prod; only visible branding is the deterministic template bake. Backend schema is plain `z.string()` (not `.url()`) at :324/:1460. **For the vision-describe fix, Gemini text/vision is ALSO on the billing-held project, so the describe call must default to OpenAI (gpt-4o-mini), NOT the Gemini-first chain.**

### Root cause 1 [confidence: high] [verdict: CORRECTED]
**CLAIM:** isPublicImageUrl validates only scheme+host (any public https host passes), so an Instagram POST PAGE URL (text/html, not an image) is accepted as a valid 'style reference image'.
**EVIDENCE:**
- `packages/ai/src/utils/safe-fetch-url.ts:90-106` — `isPublicImageUrl`: allows `data:image/...;base64,`, requires https, blocks private/loopback/metadata, then returns true for any public host
- `packages/api/src/routers/repurpose.router.ts:322-326` — `aestheticRefUrl: z.string().optional();` `imageContext: z.string().max(300).optional()`
**PROPOSED FIX:** Add a `safeFetchPublicImage(url, {maxBytes, timeoutMs})` → {base64, mimeType} helper to safe-fetch-url.ts that (1) gates on isPublicImageUrl, (2) fetches with redirect:"manual" + AbortSignal.timeout (mirror safeFetchImage 113-124), (3) REJECTS unless content-type matches `/^image\/(png|jpe?g|webp|gif)/`, (4) caps body bytes (~8MB). Tighten schema to `aestheticRefUrl: z.string().url().optional()` at :324 and :1460.
**VERIFIER (refuted=False):** Verified verbatim. Both consumption sites (528-539 main, 1486-1519 regenerate) gate only on isPublicImageUrl, then bare fetch(), take content-type on faith, base64 the whole body with no cap, push into Gemini referenceImages. Schema is `z.string().optional()` (not even .url()). **CORRECTION:** isPublicImageUrl validates scheme+host AND accepts inline data:image base64 (blocking private/loopback/metadata); for remote URLs any public https host passes. The actual fetches (router:530, :1510) use bare fetch() with NO redirect:"manual", NO timeout, NO byte cap — weaker than safeFetchImage — so the issue includes redirect-based SSRF exposure and unbounded body reads on top of the missing content-type check.

### Root cause 2 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The router's reference fetches (main flow lines 509-539, regenerateImage lines 1506-1519) perform a raw `fetch` with NO Content-Type validation, NO size cap, NO timeout — for an Instagram page the response is text/html, and the code base64-encodes the HTML bytes and pushes { base64, mimeType: "text/html; charset=utf-8" } into brandReferenceImages; nano-banana forwards that mime verbatim as Gemini inline_data, which would 400 (unsupported mime) if Gemini were reachable.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:528-539` — `const r = await fetch(input.aestheticRefUrl); ... const mime = r.headers.get("content-type") || "image/png"; const b64 = Buffer.from(await r.arrayBuffer()).toString("base64");`
- `packages/api/src/routers/repurpose.router.ts:1507-1515` — `for (const refUrl of [safeLogoUrl, safeAestheticRef]) { ... const r = await fetch(refUrl); ... }`
- `packages/ai/src/providers/nano-banana.provider.ts:76-86` — `inline_data: { mime_type: ref.mimeType || "image/jpeg", ... }`
**PROPOSED FIX:** Replace the three inline fetch blocks (router:509-520 logo, :528-539 aesthetic, :1507-1519 regenerate) with the new safeFetchPublicImage helper. On content-type !== image/*, do NOT silently continue: if text/html, run og:image fallback (next RC), otherwise emit `progress("Style reference", "skipped", "URL is not an image")`.
**VERIFIER (refuted=False):** All three snippets match exactly; bare fetch, no timeout/redirect:manual/size-cap/content-type. The Gemini-400-on-text/html part is the only non-code-verifiable element (plausible inference; HTML is broken reference input regardless). **CORRECTION:** Accurate. (a) "Gemini would 400" is an inference — Gemini accepts some text/* mime types on standard models, but gemini-3.1-flash-image-preview with imageConfig very likely rejects text/html and it's garbage conditioning anyway; (b) the proposed `safeFetchPublicImage` helper does NOT yet exist — the existing helper is `safeFetchImage`, which enforces a strict S3-only allowlist and would break external-CDN logo/aesthetic URLs if reused; a new public-host variant must be added.

### Root cause 3 [confidence: high] [verdict: CORRECTED]
**CLAIM:** A social PAGE URL never yields the post's actual image because there is no og:image extraction step — even though the codebase already has the exact helper pattern (getMeta/getImages in url-extractor.ts) used elsewhere for og:image.
**EVIDENCE:**
- `packages/ai/src/utils/url-extractor.ts:247-252` — `getImages`: `const og = getMeta(html, "og:image"); if (og) images.push(og);` then twitter:image
- `packages/ai/src/utils/url-extractor.ts:221-225` — `getMeta` regexes for `property="..."` content
**PROPOSED FIX:** Export `resolveImageFromPageUrl(url): Promise<string|null>` from url-extractor.ts (wraps getMeta(html,"og:image")/"twitter:image" over a size-capped HTML fetch). In the router, when the aestheticRefUrl fetch returns text/html, call it, re-validate the resolved URL with isPublicImageUrl, then safeFetchPublicImage it. Caveat: instagram.com frequently serves a login interstitial to anonymous fetchers — on null surface an explicit message ("couldn't read an image from that link — upload the image instead").
**VERIFIER (refuted=False):** All locations verified. getMeta/getImages match; getMeta(html,"og:image") used at lines 364/412/462/513/534/609/637/696; both helpers are module-private (not exported), which the fix correctly accounts for. Both router sites (528-539, ~1487-1520) do raw fetch gated only by isPublicImageUrl; no og:image extraction anywhere (grep zero). Behavior is worse than "no image" — HTML pushed wholesale as a bogus text/html "reference". **CORRECTION:** (a) the fix must patch BOTH router sites; (b) the shipped UI (RepurposeTab.tsx ~767-806) populates aestheticRefUrl exclusively via file upload (no URL-paste field), so the "user pastes IG link" scenario is only reachable via direct API calls/future feature — real backend gap, not a currently-triggerable UI bug; (c) new resolveImageFromPageUrl export should add a content-type gate + redirect:"manual" (current line-530 fetch follows redirects).

### Root cause 4 [confidence: high] [verdict: CORRECTED]
**CLAIM:** Reference images are Gemini-only by construction: generateImageSafe's non-safety fallback calls generateImageDallE with prompt+size ONLY (references silently dropped), and gpt-image-1 has no image-input path — and since the prod Gemini billing-403 message does NOT match isSafetyBlock, attempt 1 always takes that non-safety branch, so in production the aesthetic/logo references NEVER influence the generated background (the feature is invisible).
**EVIDENCE:**
- `packages/ai/src/utils/safe-image-generator.ts:148-163` — non-safety catch → "Falling back to DALL-E" (references dropped)
- `packages/ai/src/utils/safe-image-generator.ts:126-129` — `isSafetyBlock`: `/blocked|safety|IMAGE_OTHER|PROHIBITED_CONTENT|SAFETY|content[_\s]?policy|policy_violation/i`
- `packages/ai/src/providers/dalle.provider.ts:103-111` — body = model/prompt/n/size/quality (no image param)
**PROPOSED FIX:** Provider-agnostic style transfer: after assembling brandReferenceImages (router:~539) and before the slide loop, vision-describe the aesthetic ref ONCE via an OpenAI vision model (the multimodal image_url plumbing exists in chat-agent.chain.ts lines 11 + 289-290) with a prompt like "Describe this image's visual style in <=40 words: color palette, lighting, composition, mood, medium. No subject description." Append the descriptor to bgPrompt via existing appendImageContext (router:116-120). Reuse for every carousel slide (compute once). Keep passing referenceImages so Gemini conditioning resumes when billing is fixed. Mirror in regenerateImage (:1526-1529).
**VERIFIER (refuted=False):** All snippets verbatim. Ran the actual isSafetyBlock regex against the prod message ("Lightning dunning decision is deny ... PERMISSION_DENIED") — no match → attempt 1 always goes to DALL-E without references. References consumed only by attempts 1/2 (Gemini); attempt 3 drops them; attempt 4 is DALL-E. Fix anchors verified. **CORRECTION:** (1) what's dead is the references' influence on the AI BACKGROUND — the logo is still visible via the deterministic template bake; references are attempts-1/2-only by construction. (2) chat-agent.chain.ts:289-290 is the GEMINI branch (inlineData); the OpenAI/LangChain multimodal pass-through is line 246 (`new HumanMessage({content: m.content})`). regenerateImage starts at :1448, referenceImages assembly :1506-1519, appendImageContext bgPrompt :1526-1530 — ":1526-1529" is the right insertion point.

### Root cause 5 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The UI offers NO URL input for the style reference — only a file upload (input type=file accept="image/*" → /api/upload → S3 URL) — so a user 'pasting an Instagram URL as the style reference' can only put it in the imageContext textarea, where appendImageContext injects the raw URL as literal prompt text ('Style notes: https://www.instagram.com/p/...'), which the image model cannot fetch and may even render as text in the image.
**EVIDENCE:**
- `apps/web/components/content-agent/RepurposeTab.tsx:769-784` — hidden `<input type="file" accept="image/*" id="aesthetic-ref-upload">`
- `apps/web/components/content-agent/RepurposeTab.tsx:807-815` — "Aesthetic / style notes" `<Textarea id="image-context">`
- `packages/api/src/routers/repurpose.router.ts:116-120` — `appendImageContext` returns `${basePrompt}\n\nStyle notes: ${notes.slice(0, 300)}`
**PROPOSED FIX:** In RepurposeTab.tsx (next to the upload button, 768-805) add a small "or paste image/post URL" text input that sets aestheticRefUrl; client-side, if the host is a known social domain / lacks an image extension, show "we'll grab the post's image automatically" (backed by the og:image server fix). Additionally strip bare URLs out of imageContext before submit (or warn "links don't work here") so URLs stop leaking into bgPrompt via appendImageContext.
**VERIFIER (refuted=False):** Every element confirmed. `setAestheticRefUrl` called in exactly two places (upload handler set, Clear button reset). appendImageContext verbatim; applied at router:848/1263-1265/1526-1528; imageContext sent from UI at 384/446. Traced `sanitizePrompt` (safe-image-generator.ts:63-97, applied at 169/194) — it does NOT strip URLs (targets quoted Title-Case/political/celebrity terms), so a lowercase URL passes through intact; neither image model fetches URLs ("may render as text" appropriately hedged). **CORRECTION:** Accurate. Gap is purely UI-side — the tRPC mutations already accept aestheticRefUrl (router:324/:1460, fetched behind isPublicImageUrl :528-530), so a pasted URL has a ready backend path; the literal-URL leak survives all downstream sanitization.

### Root cause 6 [confidence: high] [verdict: confirmed — no verifier assigned]
**CLAIM:** Security gap (secondary): all three reference fetches use bare fetch() with default redirect-follow and no timeout, so the isPublicImageUrl SSRF check can be bypassed by a public URL that 302-redirects to a private/metadata host — unlike safeFetchImage which already uses redirect:"manual" + timeout; bodies are also unbounded (multi-MB base64 into the Gemini request).
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:528-531` — `const r = await fetch(input.aestheticRefUrl);`
- `packages/ai/src/utils/safe-fetch-url.ts:113-124` — `safeFetchImage(... opts?: {timeoutMs?})` with isAllowedImageUrl gate
**PROPOSED FIX:** Fold into the safeFetchPublicImage helper (RC1): redirect:"manual" (treat 30x as failure, or re-validate Location against isPublicImageUrl for ≤1-2 hops), AbortSignal.timeout(10s), hard byte cap. Apply at router:511 (logo), :530 (aesthetic), :1510 (regenerate). Add a regression test alongside `packages/ai/src/__tests__/image-fetch-ssrf.test.ts` asserting a redirect-to-169.254.169.254 is not followed and text/html is rejected.
**VERIFIER:** No matching verifier found (this RC was not independently verified; note RC1/RC2 verifiers already corroborated the missing redirect/timeout/cap).

---

## ISSUE: regenerate — per-image "Regenerate" produces blank/wrong images (esp. hook_bars)
*(agent-a58d695066f7866b4)*
**NOTES:** regenerateImage DOES honor styleNeedsAiBackground (delegates to shared renderStaticCreative; gate at line 170 skips generateImageSafe for hook_bars/bold_typographic) — the bg skip is NOT the regression; the missing hook bar (RC1) + raw headline (RC2) are. slideRole/body omission is benign (UI only exposes Regenerate for the static image and the carousel COVER slide 0). secondaryImageUrl/subhead/tag/verified are pre-existing dead options, not regenerate-specific. The Puppeteer renderer is fully shared (400ms paint delay) so Phase-2a blank-paint is not the cause. Prod billing hold → premium/tweet_card regen backgrounds come from gpt-image-1 fallback (refs ignored), same as main flow. **Suggested order: RC2 (return renderedHeadline+hookLine from repurposeFromUrl, store in results, resend) fixes both RC1 and RC2 with one wire change; RC3/RC4 are small additive schema/helper changes.**

### Root cause 1 [confidence: high] [verdict: CORRECTED]
**CLAIM:** regenerateImage never generates nor accepts a hookLine, so a hook_bars regenerate renders WITHOUT its signature hook bar — and since styleNeedsAiBackground('hook_bars') is false (correctly honored), the result is a flat #f7f7f8 (light-theme bgFallback) canvas with a single white bar: the 'blank image' the user sees.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:1531-1546` — `creative = await renderStaticCreative({ ai:{...}, bgPrompt, headline, category:"news", creativeStyle: input.creativeStyle, ... })` (no hookLine)
- `packages/api/src/routers/repurpose.router.ts:855-865, 874` — main static flow generates hookLine gated on hook_bars and passes it
- `packages/ai/src/tools/creative-templates.ts:188-216` — buildHookBars: hook bar only rendered when opts.hookLine present; no-photo fallback = flat theme bg
- `packages/api/src/routers/repurpose.router.ts:54-56 + 167-176` — styleNeedsAiBackground; gate `if (args.slideRole !== "cta" && styleNeedsAiBackground(args.creativeStyle))`
**PROPOSED FIX:** In regenerateImage (router:1465+), when creativeStyle === 'hook_bars', generate a hook line like the main flow (generateContent with the 4-7-word **highlight** prompt, then capHookLine which is already module-level at line 65) and pass `hookLine` into the renderStaticCreative call at 1533. Alternatively/additionally: return the hookLine from repurposeFromUrl (1420-1437), store in results state, add optional `hookLine: z.string().optional()` to the input schema (1450-1463), and have handleRegenerate resend it — avoids an extra AI call.
**VERIFIER (refuted=False):** Every checkable element holds. Input schema has no hookLine; mutation body never generates one; render call omits it. styleNeedsAiBackground false for hook_bars; bgImageUrl never passed. Main flow generates+caps the hook (855-869, capHookLine@65) and passes it (874→584). repurposeFromUrl response (1420-1437) doesn't include hookLine. Note: regenerateImage's input has no `provider` field, so in-mutation hook generation needs a default provider. **CORRECTION:** (a) "Blank image" slightly overstates — it renders the logo/initial avatar + ONE white bar with the headline in dark #111 44px/800; reads washed-out/near-empty but the headline is legible. (b) Flat #f7f7f8 applies only for theme="light" (default); dark/gradient fall back to gradients. The flat no-photo bg is hook_bars' design in the MAIN flow too — the regenerate-specific regression is solely the missing hook line/bar and its accent-color highlight.

### Root cause 2 [confidence: high] [verdict: CORRECTED]
**CLAIM:** Headline parity break: the UI sends the RAW extracted page title (results.extracted?.title) and the server applies no capHeadline / generic-title substitution / social-headline synthesis — but the original creative was rendered with `headlineForCreativeFinal` (AI SUBJECT for generic titles, AI-synthesized for social URLs, capped to 12 words/80 chars), and the router never returns that final headline to the client — so regenerated images carry long/generic/garbled headlines ('wrong image'), overflowing templates whose font sizing assumes capped input.
**EVIDENCE:**
- `apps/web/components/content-agent/RepurposeTab.tsx:366-373` — `const headline = (results.extracted?.title || "").trim();`
- `packages/api/src/routers/repurpose.router.ts:806-836` — briefSubject substitution + social synthesis + capHeadline (none in regenerateImage; :1521 `const headline = input.headline.trim();`)
- `packages/api/src/routers/repurpose.router.ts:1420-1437` — response returns only RAW extracted.title (headlineForCreativeFinal/hookLine NOT returned)
- `packages/api/src/routers/repurpose.router.ts:736-741` — capHeadline is a CLOSURE inside repurposeFromUrl (unreachable from regenerateImage); carousel cover caps at 1220
**PROPOSED FIX:** Return the headline actually rendered (`headlineForCreativeFinal` for static, `coverHeadline` for carousel) in the repurposeFromUrl response (e.g. `renderedHeadline` at 1420-1437 and reel return at 1376-1398); store on UI results state (118-127) and use in handleRegenerate (369) instead of `results.extracted?.title`. Defensively hoist `capHeadline` to module level (next to capHookLine at router:65) and apply it in regenerateImage at line 1521.
**VERIFIER (refuted=False):** Every line verified verbatim. capHeadline is a closure inside repurposeFromUrl (unreachable). Original static used headlineForCreativeFinal (806-836, rendered @874); carousel cover used capHeadline(extracted.title) @1220. Both responses expose only raw extracted.title. regenerateImage :1521 = `const headline = input.headline.trim();` uncapped. **CORRECTION:** (a) when the title is specific (non-generic, non-social) the original used capHeadline(extracted.title), so regeneration diverges only by the missing cap (overflow); substitution/synthesis divergence applies only to generic/social sources. (b) UNDERSTATED carousel case: handleRegenerate for a non-cover slide (index ≥ 1) also sends raw page title, but body slides were originally rendered with per-slide AI titles (slideData[i].title) and CTA with "Follow for More" — returning coverHeadline only restores parity for the cover; regenerating body/CTA slides replaces them with a static-style page-title creative regardless.

### Root cause 3 [confidence: medium] [verdict: CORRECTED]
**CLAIM:** Background-prompt parity break: regenerateImage builds its AI bg prompt from the headline ALONE — the main flow's prompt includes the full contentBrief + a 400-char content summary + disambiguation instruction — and the input schema has no field for any article context, so premium_editorial/tweet_card regenerations get off-topic/wrong backgrounds.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:1523-1529` — `bgPrompt = appendImageContext(\`...Topic: "${headline}"...\`, input.imageContext)` (topic = headline only)
- `packages/api/src/routers/repurpose.router.ts:838-848` — main flow: `${contentBrief}` + `Context: ${contentSummary.slice(0,400)}` + who/what disambiguation
- `packages/api/src/routers/repurpose.router.ts:1449-1463` — input schema has NO context/description/brief field
**PROPOSED FIX:** Add optional bounded `bgContext: z.string().max(600).optional()` to regenerateImage input (1450-1463) and interpolate into the bg prompt at 1526-1529 (flows through sanitizePrompt already); in handleRegenerate (377-388) send `bgContext: results.extracted?.description?.slice(0, 600)`. Better: also return the original `contentBrief` (or exact bgPrompt) from repurposeFromUrl and resend.
**VERIFIER (refuted=False):** All four snippets byte-for-byte. Affected styles precise (AI bg only for premium_editorial/tweet_card). Claim actually understates: regenerate uses raw extracted.title while the main flow swaps in contentBrief SUBJECT for generic titles (806-816) / synthesizes for social (819-835). **CORRECTION:** (a) imageContext (z.string().max(300)) could in principle carry context but is used/documented as user style notes and the UI never populates it from extracted.description; (b) parity break is wider than backgrounds — regenerate's headline is raw extracted.title whereas the main flow may have used contentBrief SUBJECT or a synthesized social headline.

### Root cause 4 [confidence: medium] [verdict: CORRECTED]
**CLAIM:** Logo/brand parity break: regenerateImage only honors input.logoUrl, skipping the main flow's DB resolution chain (media-library `category:"logo"` row → channel metadata.logo_path → channel avatar), and the UI's resolveBranding only falls back to `logoUrl || ch?.avatar` — so when the original creative's logo came from the saved media-library logo, the regenerated image renders with a different logo (or none) AND a different derived brandColor.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:1484-1500` — `safeLogoUrl = input.logoUrl && isPublicImageUrl(...) ? input.logoUrl : undefined;` brandColor derived only from that
- `packages/api/src/routers/repurpose.router.ts:411-435` — main flow: input.logoUrl → DB logo media `findFirst({ where:{ organizationId, category:"logo", channelId }})` → metadata.logo_path → channel avatar
- `apps/web/components/content-agent/RepurposeTab.tsx:355-364` — resolveBranding fallback only state logoUrl → channel avatar
**PROPOSED FIX:** Extract the main flow's logo-resolution block (router:411-454) into a shared helper and call it from regenerateImage before line 1484, passing channelName/channelHandle (already in the input schema) so both paths resolve identical logo + derived brandColor.
**VERIFIER (refuted=False):** All three locations read; chain confirmed; brandColor effects confirmed in creative-templates.ts (hook_bars highlight 195/210, gradient bgFallback ~109-110, bold_typographic accent-band 275/282). **CORRECTION:** Narrower trigger window: (a) the main-flow UI ALSO front-runs the DB chain by sending `logoUrl || ch?.avatar || ""` (RepurposeTab.tsx:435-440), so the saved media-library logo / metadata.logo_path is used only when the channel has NO avatar and no session-uploaded logo — in that scenario the regenerated image renders with NO logo (not a different one) + default accent; (b) channel-bound category:"logo" rows are created by the NewsGrid logo library (newsgrid.router.ts assignLogo ~567-587), not the RepurposeTab uploader (omits channelId, line 717) — affected population is users who assigned a logo-library logo to an avatar-less (typically token-based) channel; (c) derived-brandColor divergence only when accentColor is unset (default). The proposed shared-resolver fix is sound.

---

## ISSUE: carousel — slideCount+2 slides, near-identical "same person" backgrounds, blank-white on failure
*(agent-a6d8871aaa741fa28)*
**NOTES:** Blank-white is NOT the old paint-race bug (generateStyledCreativeImage already has the 400ms paint delay + screenshot-on-timeout); it's purely the no-bgImageUrl + light-theme flat #f7f7f8 fallback. Prod billing-403 doesn't match isSafetyBlock → prod always takes attempt-1 non-safety → direct gpt-image-1 with the original UNSANITIZED prompt; the "no recognizable real people" note (sanitizePrompt) is never applied in prod and references are never forwarded to the OpenAI fallback. styleNeedsAiBackground means hook_bars/bold_typographic carousels NEVER get an AI bg. CTA slides always skip AI bg. For Issue 1, the UI footnote at RepurposeTab.tsx:863 does disclose the +2, so it's a semantics/product decision; **enforceSlideCount unit tests (packages/api/src/__tests__/repurpose-slidecount.test.ts) need updating if router semantics change.** The slidePrompt is at router:1166-1176 and also uses input.slideCount — must change together with lines 1204 and 1213.

### Root cause 1 [confidence: high] [verdict: CORRECTED]
**CLAIM:** slideCount is defined and enforced as the number of CONTENT slides only; the carousel branch then prepends a cover slide and appends a 'Follow for More' CTA slide, so a user selection of 3 produces slideCount+2 = 5 total slides, while the UI segmented control is labeled 'Content slides' with only a 10px footnote disclosing the +2 — users read the number as total.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:327-329` — `slideCount: z.number().int().min(3).max(10).default(5)` (comment: total = slideCount + 2)
- `packages/api/src/routers/repurpose.router.ts:1209-1213` — `slideData = enforceSlideCount(slideData, input.slideCount, fallbackSlidesFromSentences)`
- `packages/api/src/routers/repurpose.router.ts:1219-1225` — `allSlides = [{cover}, ...slideData.map content, {cta, "Follow for More"}]`
- `apps/web/components/content-agent/RepurposeTab.tsx:846-863` — `<Label>Content slides</Label>` + `[3,5,7,10]` buttons + 10px footnote
- `apps/web/components/content-agent/RepurposeTab.tsx:99, 450` — `useState<number>(5)` sent as `slideCount`
- `packages/api/src/routers/repurpose.router.ts:85-94` — `enforceSlideCount` (pure, exported, "Does NOT touch cover/cta")
**PROPOSED FIX:** Make slideCount mean TOTAL slides. In the carousel branch: `const contentCount = Math.max(1, input.slideCount - 2);` and use it everywhere input.slideCount is used in that branch — slidePrompt (1166), fallback slice (1204), enforce call (1213 `enforceSlideCount(slideData, contentCount, ...)`). Update schema comment (327-329; min(3) still valid: 3 total = cover+1 content+cta). In the UI change Label (848) to 'Total slides' and footnote (863) to 'Includes a cover and a follow-for-more slide.'. Update enforceSlideCount unit tests. Alternative: keep router semantics, fix only UI label/footnote — but the bug report favors total-count.
**VERIFIER (refuted=False):** Every location byte-for-byte; selecting 3 yields 5 rendered slides; allSlides.length drives generation (1227-1228); 10px footnote literal; tests exist (repurpose-slidecount.test.ts). **CORRECTION:** The "carousel branch" (router:1164) actually handles BOTH carousel and reel, and the slide-count picker is carousel-only — so reel always runs with default slideCount=5 (7 total). Any fix changing slideCount to mean total inside that branch will also shrink reel slideshow output (7→5) unless gated to `format === "carousel"`.

### Root cause 2 [confidence: high] [verdict: CORRECTED]
**CLAIM:** Every carousel slide's AI background prompt is ~95% identical: only the 3-6-word slide title varies while the full shared contentBrief (incl 'SUBJECT: <full name and identity>' and a single 'VISUAL: <ideal image scene>') is appended to EVERY slide's prompt, the same renderStaticCreative suffix/theme descriptor and the same brandReferenceImages condition every slide, and nothing instructs per-slide variety; additionally the image path has NO unconditional real-person guard — sanitizePrompt's 'do NOT include recognizable real people' note is appended ONLY after a Gemini SAFETY block (attempts 2/4), and the production path (Gemini billing-403 = non-safety) sends the ORIGINAL unsanitized prompt straight to gpt-image-1 — unlike the Seedance prompt which always forbids real named figures.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:1262-1266` — `bgPrompt = appendImageContext(\`Cinematic background photo for: "${slideMeta.title}". ${contentBrief}\`, input.imageContext)`
- `packages/api/src/routers/repurpose.router.ts:692-697` — contentBrief = `SUBJECT: ... CONTEXT: ... CATEGORY: ... TONE: ... VISUAL: ... KEYWORDS: ...`
- `packages/api/src/routers/repurpose.router.ts:170-184` — renderStaticCreative themeBgDescriptor/suffix
- `packages/api/src/routers/repurpose.router.ts:576-583` — same creativeStyle/theme/logo passed per slide
- `packages/ai/src/utils/safe-image-generator.ts:144-162` — attempt 1 original prompt via Gemini; non-safety → DALL-E
- `packages/ai/src/utils/safe-image-generator.ts:92-96` — safety note only appended in sanitizePrompt
- `packages/ai/src/utils/safe-image-generator.ts:126-129` — isSafetyBlock regex
- `packages/ai/src/providers/seedance.provider.ts:277` — unconditional "Do NOT depict any specific, real, named public figure"
**PROPOSED FIX:** (a) Variety: in the carousel branch (1262-1266) build a per-slide prompt using the slide's OWN content — include `slideMeta.body`, a `Slide ${slideIdx+1} of ${allSlides.length}` marker, a rotated composition descriptor (e.g. `const ANGLES = ['wide establishing shot','close-up detail/texture','overhead flat-lay','abstract geometric pattern','environment/location shot']; ANGLES[slideIdx % ANGLES.length]`) plus an explicit 'Make this scene visually DISTINCT from the other slides' instruction; pass only CATEGORY/TONE of contentBrief (via the /CATEGORY:/ regex at 1244) rather than the whole brief with SUBJECT/VISUAL. (b) People guard: append an unconditional likeness clause mirroring seedance.provider.ts:277 inside renderStaticCreative (router:179) — because sanitizePrompt only fires after a Gemini SAFETY block, and the prod path (billing 403, no isSafetyBlock match) forwards the raw 'SUBJECT: <full named person>' prompt to gpt-image-1 at 154-158 with no guard.
**VERIFIER (refuted=False):** Read every location; shared contentBrief appended per slide, slidePrompt constrains titles to 3-6 words, identical suffix (179) + brandReferenceImages (583), zero variety instructions (grep). sanitizePrompt only in attempts 2/4; billing-403 matches no isSafetyBlock term → non-safety branch to gpt-image-1. seedance:277 unconditional. **CORRECTION:** (1) "Every slide" → every COVER and BODY slide when style needs AI bg (premium_editorial/tweet_card); CTA skips it (slideRole gate 170); hook_bars/bold_typographic skip for all slides. (2) The cover's varying token is capHeadline(extracted.title) (~12 words), not 3-6 words. (3) The prompt forwarded to gpt-image-1 on the non-safety path is the original plus only enforceNoHashtags (safe-image-generator.ts:141) — no people/likeness sanitization.

### Root cause 3 [confidence: high] [verdict: CORRECTED]
**CLAIM:** When per-slide AI background generation throws (e.g. Gemini billing 403 followed by an OpenAI gpt-image-1 failure such as a 429 from the 3-concurrent-call batches), renderStaticCreative's catch merely logs and leaves backgroundImageUrl undefined, so generateStyledCreativeImage is called WITHOUT bgImageUrl and the template falls back to themeTokens' bgFallback, which on the default 'light' theme is flat near-white '#f7f7f8' — i.e. a blank white slide; there is NO stock photo, gradient-accent, or article-image fallback despite the doc comment claiming 'a stock background'; the carousel cover takes the exact same path; CTA slides always skip AI bg by design.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:163-190` — `let backgroundImageUrl: string | undefined; let bgSource:"ai"|"stock" = "stock"; ... try {...}`
- `packages/api/src/routers/repurpose.router.ts:192-206` — `...(backgroundImageUrl ? { bgImageUrl: backgroundImageUrl } : {})`
- `packages/ai/src/tools/creative-templates.ts:115-123` — light `bgFallback: "#f7f7f8"`
- `packages/ai/src/tools/creative-templates.ts:158-161 / 301-304` — `const safeBg = safeImageUrl(opts.bgImageUrl); const bg = safeBg ? background-image : \`background:${tokens.bgFallback};\``
- `packages/api/src/routers/repurpose.router.ts:335` — `theme: ....default("light")`
- `packages/ai/src/utils/safe-image-generator.ts:154-162` — DALL-E fallback then rethrow
- `packages/api/src/routers/repurpose.router.ts:1242-1243, 1254-1257` — `BATCH_SIZE = 3; DELAY_BETWEEN_BATCHES = 3000`; Promise.all batches
- `packages/api/src/routers/repurpose.router.ts:128-133` — doc comment falsely claiming a stock background
**PROPOSED FIX:** Give the failure path a real visible fallback, in order of preference: (1) **Article-image fallback** — `extracted.images` already returned (router 1148/1382/1426); in the carousel branch pass `extracted.images[0]` (gated by isPublicImageUrl) into renderStaticCreative as a `fallbackBgUrl` arg, and in the catch (187-189) set `backgroundImageUrl = fallbackBgUrl`. (2) Make the light no-image fallback non-blank — in each builder (buildPremiumEditorial 159-161, buildBodyChrome 302-304), when `!safeBg`, substitute an accent-derived gradient (reuse the gradient theme pattern at line 110) instead of `tokens.bgFallback`. (3) Surface honesty — renderStaticCreative already returns `bgSource` (161/207) but the carousel branch discards it (1267-1278); propagate per-slide bgSource into progress events so the UI can warn 'backgrounds unavailable'. Also fix the misleading doc comments at 128-133 and 188. Note: the cover has no separate failure path (allSlides[0] in the same loop); the outer per-slide catch (1279-1282) fires only on Puppeteer render throws; AI-bg failure is swallowed INSIDE renderStaticCreative so the slide still uploads — blank.
**VERIFIER (refuted=False):** All verified within ±1 line. No stock/article-image fallback exists; doc comments at 128-133 and 541-548 falsely promise 'a stock background'. BATCH_SIZE=3 batches real. Cover is allSlides[0]; CTA skips; bgSource discarded at 1278; AI-bg failure swallowed inside renderStaticCreative so the flat slide still uploads. **CORRECTION:** Not a literally blank white slide — the template still bakes the headline (#0f1419), italic brand label, accent rule, logo/initial badge, @handle onto the flat #f7f7f8 — a flat, photo-less-but-branded creative. Dark/gradient themes have gradient bgFallbacks (creative-templates.ts:103/110); flat #f7f7f8 is the default-light outcome only.

---

## ISSUE: static-blank — hook_bars/bold_typographic static creative renders near-blank white
*(agent-a9c23c1f9be382d9e)*
**NOTES:** The 'stock background' wording in renderStaticCreative's doc-comment (router:128-132) and the progress detail at line 890 ('stock background — AI image unavailable') are misleading — there is NO stock photo; the fallback is the flat themeTokens.bgFallback color. Both render entry points share renderStaticCreative: buildHeadlineCreative closure (571) and standalone regenerateImage (1533) — a signature fix covers both, but regenerateImage needs its own bgImageUrl input (no `extracted` context). safeImageUrl (creative-templates.ts:59-63) rejects http:// (https/data-image only) — consider fetching the article image server-side (through isPublicImageUrl, like the logo fetch) and inlining as a data URL. The Puppeteer renderer already handles large data-URL backgrounds. theme defaults to 'light' in schema (335) and builder (creative-templates.ts:189). The CTA-slide skip (line 170) is intentional and should be preserved.

### Root cause 1 [confidence: high] [verdict: **REFUTED**]
**CLAIM:** styleNeedsAiBackground() returns false for hook_bars/bold_typographic, so renderStaticCreative never sets backgroundImageUrl and the conditional spread omits bgImageUrl entirely — the template gets NO background of any kind for these styles.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:54-56` — `styleNeedsAiBackground` returns false for hook_bars/bold_typographic
- `packages/api/src/routers/repurpose.router.ts:163-170` — backgroundImageUrl init; text-first comment
- `packages/api/src/routers/repurpose.router.ts:192-206` — `...(backgroundImageUrl ? { bgImageUrl } : {})`
**PROPOSED FIX:** renderStaticCreative (141-208) has no input for a pre-existing background image; add optional `bgImageUrl?: string` arg, initialize `backgroundImageUrl = args.bgImageUrl` (so text-first styles get a real photo when one exists), keep the AI-generation block as an override only when styleNeedsAiBackground is true. Set bgSource accordingly. The conditional spread at 204 forwards it.
**VERIFIER (refuted=TRUE):** The MECHANISM is accurate (styleNeedsAiBackground false; only backgroundImageUrl assignment @185 is gated; args type has no bg-image input; spread omits bgImageUrl). **BUT the load-bearing conclusion — "the template gets NO background of any kind" — is FALSE.** In creative-templates.ts, buildHookBars (~187-193) falls back to `background:${tokens.bgFallback}` when bgImageUrl is absent, and buildBoldTypographic NEVER reads opts.bgImageUrl — it always renders `background:${tokens.bgFallback}` plus an accent band. themeTokens (99-125) guarantees a non-empty bgFallback for every theme (dark: navy gradient; gradient: brand-color gradient; light: #f7f7f8). The doc comment at router:44-52 documents this as intentional ("text-first styles ... Task 1 gave them solid/gradient fallbacks ... latency win"). So these styles ALWAYS produce a painted background by design and cannot produce a blank image via this path; the proposed fix is also ineffective for bold_typographic (its template discards bgImageUrl). **CORRECTION:** The proposed fix would only affect hook_bars (whose .bg div honors bgImageUrl); bold_typographic would silently discard it without template changes, and adding a photo contradicts these styles' text-first design.

### Root cause 2 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The extracted article og:image (extracted.images[0], which url-extractor populates first from og:image) is NEVER plumbed into the static render path — buildHeadlineCreative passes only the text prompt/headline, and extracted.images is used solely in JSON return payloads — so even when the source article (NDTV) has a real photo, hook_bars renders photoless.
**EVIDENCE:**
- `packages/ai/src/utils/url-extractor.ts:6-16, 246-253` — `images: string[]`; getImages pushes og:image first
- `packages/api/src/routers/repurpose.router.ts:871-874` — `const creative = await buildHeadlineCreative(bgPromptWithContext, headlineForCreativeFinal, category, hookLine);`
- `packages/api/src/routers/repurpose.router.ts:571-588` — buildHeadlineCreative body delegates to renderStaticCreative
- `packages/api/src/routers/repurpose.router.ts:1148, 1382, 1426` — only uses of `extracted.images` (all return payloads)
**PROPOSED FIX:** In the static branch (791-899) and buildHeadlineCreative (549-589), resolve `const articleImageUrl = extracted.images.find(u => isPublicImageUrl(u))` (isPublicImageUrl already destructured at 377, used at 451/528) and pass it through to renderStaticCreative as the new bgImageUrl arg from RC1. Note safeImageUrl accepts only https://or data:image — http:// og:images dropped (upgrade-to-https or fetch-and-inline as data URL). Optionally use it as the AI-bg-failure fallback (187-189) for premium_editorial/tweet_card. regenerateImage (1448-1546) has no article context — accept an optional isPublicImageUrl-validated `bgImageUrl` input there too, else hook_bars regenerates stay blank.
**VERIFIER (refuted=False):** Every location verified. extracted.images used ONLY at 1148/1382/1426 (return payloads). Static path builds text-only bgPrompt; renderStaticCreative arg type has NO bgImageUrl field; the only bgImageUrl forwarded is the internal AI-generated data URL @185. hook_bars photoless is over-determined: (a) styleNeedsAiBackground skips generateImageSafe; (b) buildHookBars DOES consume opts.bgImageUrl (190-193) and a circular secondaryImageUrl inset (196-198) but NO caller supplies secondaryImageUrl. safeImageUrl regex `^(https:\/\/|data:image\/(png|jpeg|jpg|webp|gif);base64,)[^"')\s<>\\]+$` rejects http:// (doc comment at line 34 says "http(s) URL" — wrong). regenerateImage has no article-image/bgImageUrl field. **CORRECTION:** hook_bars is photoless for TWO stacked reasons (article image never plumbed + styleNeedsAiBackground skips AI bg). Two fix refinements: (a) when passing an article photo to hook_bars, bypass/ignore styleNeedsAiBackground for the pass-through (it gates AI generation, not a free real photo; the article image could arguably feed secondaryImageUrl/the inset instead); (b) the safeImageUrl https-only restriction contradicts the bgImageUrl doc comment at line 34 — fix the comment or upgrade/inline http images.

### Root cause 3 [confidence: high] [verdict: CORRECTED]
**CLAIM:** With bgImageUrl undefined and theme=light (the schema default, line 335), buildHookBars falls back to a flat near-white `#f7f7f8` canvas; its white `#fff` bars on that background are nearly invisible except for box-shadow, producing exactly the reported 'blank white card with bars at bottom + tiny logo top-right'.
**EVIDENCE:**
- `packages/ai/src/tools/creative-templates.ts:187-194` — `const safeBg = safeImageUrl(opts.bgImageUrl); const bg = safeBg ? background-image : ...`
- `packages/ai/src/tools/creative-templates.ts:115-123` — light `bgFallback: "#f7f7f8"`
- `packages/ai/src/tools/creative-templates.ts:204-218` — `.bar{background:#fff;...box-shadow:0 6px 24px rgba(0,0,0,0.35);}` `.hook/.headline{...color:#111;}`
- `packages/api/src/routers/repurpose.router.ts:335` — `theme: ....default("light")`
**PROPOSED FIX:** Once RC1-2 plumb the article image in, this resolves for the normal case. For the genuinely-no-image case, give buildHookBars (187-219) a richer no-photo fallback than flat #f7f7f8 — e.g. a brand-accent gradient (reuse `linear-gradient(135deg, ${safe}, #11131a)` at line 110) or at minimum a hairline border/contrast tint on .bar so white-on-white never occurs. hook_bars is the 'viral desi-news photo + bars' style; photoless flat-white should be the exception, not the prod default.
**VERIFIER (refuted=False):** Every snippet byte-for-byte. Theme forwarded (renderStaticCreative:199 → generateStyledCreativeImage). Default logoPosition "top-right" (router:320) + 56px logo + `.bars{...bottom:48px}` match the symptom. **CORRECTION:** Mechanism confirmed but framing off: for hook_bars, bgImageUrl is ALWAYS undefined BY DESIGN (styleNeedsAiBackground excludes it), not via the RC1/RC2 plumbing failure — so **fixing RC1-2 will NOT resolve hook_bars**; the white-on-white canvas is the unconditional prod default. The correct fix is the second half of the proposal (brand-accent gradient fallback à la gradient theme token at creative-templates.ts:110, and/or hairline border/tint on .bar), AND — if hook_bars is meant to be photo+bars — adding hook_bars to the styles for which renderStaticCreative produces a background. Bars' text is #111 so the bars read via dark text + box-shadow; the "blank" part is the photo-less upper canvas.

### Root cause 4 [confidence: high] [verdict: CORRECTED]
**CLAIM:** buildBoldTypographic ignores opts.bgImageUrl ENTIRELY (it never reads it — no safeImageUrl call, no .bg div), so on theme=light it is always a flat #f7f7f8 page with a 14px accent band + headline; any background fix that only plumbs bgImageUrl will be silently ignored for this style.
**EVIDENCE:**
- `packages/ai/src/tools/creative-templates.ts:265-288` — buildBoldTypographic: `const accent = safeColor(...); const tokens = themeTokens(...);` — no bgImageUrl read, no .bg div
**PROPOSED FIX:** Decide intent: bold_typographic is plausibly text-first by design (per styleNeedsAiBackground doc-comment). If text-only, leave the builder but improve the light fallback (subtle accent-tinted gradient instead of flat #f7f7f8). If it should support a photo, add the same `safeBg ? background-image : tokens.bgFallback` + scrim pattern used by buildPremiumEditorial (158-161, 167-168). Either way, document that passing bgImageUrl to this style is currently a no-op.
**VERIFIER (refuted=False):** Verified — buildBoldTypographic never reads opts.bgImageUrl (no safeImageUrl call, no .bg div), whereas premium_editorial/hook_bars/tweet_card/buildBodyChrome all implement safeBg-or-bgFallback. light bgFallback #f7f7f8 used at line 274; .accent-band width:14px (275). **CORRECTION:** Precise: it ignores bgImageUrl for the COVER/STATIC render path (slideRole undefined or "cover"). However carousel "body"/"cta" slides under this style dispatch to buildBodyChrome (357-358) BEFORE the style switch, and buildBodyChrome DOES honor bgImageUrl (301-304) — so a bgImageUrl plumb is silently ignored only on the cover/static render, not on body/cta. buildBoldTypographic is the only builder that drops a provided background.

### Root cause 5 [confidence: high] [verdict: CORRECTED]
**CLAIM:** The carousel COVER (and body/cta slides) inherit the exact same gap: every slide routes through buildHeadlineCreative → renderStaticCreative, so for creativeStyle=hook_bars/bold_typographic the cover skips the AI background (styleNeedsAiBackground gate at line 170), no article image is passed, and buildBodyChrome's body/cta slides also fall back to the flat light bgFallback — whole carousel renders blank-light.
**EVIDENCE:**
- `packages/api/src/routers/repurpose.router.ts:1262-1277` — carousel loop calls buildHeadlineCreative per slide
- `packages/ai/src/tools/creative-templates.ts:353-363` — buildStaticCreative routes body/cta → buildBodyChrome before the style switch
- `packages/ai/src/tools/creative-templates.ts:298-304` — buildBodyChrome safeBg-or-bgFallback
**PROPOSED FIX:** The renderStaticCreative `bgImageUrl` arg from RC1 fixes the carousel automatically IF the carousel branch passes the article image: in the slide loop (1254-1283), pass the validated `extracted.images[0]` for the COVER slide (slideRole === "cover") at minimum — buildHookBars/buildBodyChrome already honor bgImageUrl. Body/cta can stay token-rendered or reuse the same photo with the existing scrim — a product decision; cite both options.
**VERIFIER (refuted=False):** All locations verified. For hook_bars/bold_typographic cover AND body slides skip generateImageSafe (cta always); renderStaticCreative has no bgImageUrl param today; extracted.images only returned to client (1148/1382/1426) never fed to renderer. buildBodyChrome falls back to #f7f7f8 (default light). **CORRECTION:** Three precisions: (1) mechanism confirmed. (2) buildBoldTypographic NEVER reads opts.bgImageUrl — passing extracted.images[0] for a bold_typographic cover is a no-op unless that builder is modified; the fix-as-written only revives hook_bars covers (premium_editorial:158 and tweet_card:231 already honor bgImageUrl and aren't gated off). (3) "blank-light" holds only for default light theme (dark/gradient fall back to gradients), slides aren't literally blank (headline bars/typography/brand chrome render), the skip is documented intentional, and the hook_bars carousel cover additionally gets hookLine=undefined (4th arg @1271) → single headline bar.

### Root cause 6 [confidence: high] [verdict: confirmed — no verifier assigned]
**CLAIM:** hook_bars' secondaryImageUrl inset is DEAD CODE: it would render a 300x300 circular photo cutout with a white border above the bars (the classic desi-news inset), but no caller anywhere in the repo ever sets secondaryImageUrl — renderStaticCreative doesn't even have a field for it, even though generateStyledCreativeImage would forward it verbatim.
**EVIDENCE:**
- `packages/ai/src/tools/creative-templates.ts:196-199` — `const safeInset = safeImageUrl(opts.secondaryImageUrl); const inset = safeInset ? \`<img class="inset-cutout" ... width:300px;height:300px;border-radius:50%;...border:6px solid #fff;...\` : "";`
- `packages/ai/src/tools/creative-templates.ts:36-37` — `/** Optional second image (hook_bars inset cutout; tweet_card image pair). */ secondaryImageUrl?: string;`
- `packages/ai/src/tools/news-image-generator.ts:151-155` — generateStyledCreativeImage spreads creativeOptions into buildStaticCreative
**PROPOSED FIX:** Wire it through: add `secondaryImageUrl?: string` to renderStaticCreative's args (141-160) and spread into the generateStyledCreativeImage call (alongside bgImageUrl at 204); producers: `extracted.images[1]` (or [0] when [0] is used as bg) in the static branch, validated with isPublicImageUrl. rg confirms the ONLY occurrences of secondaryImageUrl are creative-templates.ts:37, 196, 232 (consumer side) — zero producers. Same wiring enables tweet_card's image pair (232-238).
**VERIFIER:** No matching verifier found (this RC was not independently verified).

---

## Summary table

| Issue | Transcript | RootCauses | Verdicts |
|---|---|---|---|
| seedance-text | a01a39dc | 2 | RC1 confirmed; RC2 corrected |
| progress | a3ab3dc1 | 4 | all 4 corrected (all confirmed-true) |
| aesthetic | a48411d0 | 6 | RC1-5 corrected; RC6 confirmed (no verifier) |
| regenerate | a58d6950 | 4 | all 4 corrected |
| carousel | a6d8871a | 3 | all 3 corrected |
| static-blank | a9c23c1f | 6 | **RC1 REFUTED**; RC2-5 corrected; RC6 confirmed (no verifier) |
| **prod-logs** | a5cc36a0 | — | **TRUNCATED — no StructuredOutput; no findings available** |

**Cross-cutting facts for the implementation plan:**
- The single most important refutation: **static-blank RC1 is wrong** — hook_bars/bold_typographic are NOT background-less; they intentionally render a `themeTokens.bgFallback` (flat `#f7f7f8` on the default light theme). The real fix per the corrected RC3/RC5 is (a) a richer non-flat light-theme fallback (brand-accent gradient + bar border/tint), and (b) optionally plumbing the article og:image as `bgImageUrl` — but note bold_typographic's template discards bgImageUrl entirely, so it needs separate template work.
- "renderStaticCreative has no `bgImageUrl` arg today" is asserted and verified by both the aesthetic and static-blank investigators — adding it is the shared enabler for static-blank RC1/RC2/RC5.
- Multiple investigators independently confirmed the prod billing-403 message does NOT match `isSafetyBlock`, so in production all image generation goes straight to gpt-image-1 with the raw (unsanitized, reference-free) prompt — relevant to aesthetic RC4, carousel RC2, carousel RC3.
- A new `safeFetchPublicImage` helper (with content-type check + redirect:"manual" + timeout + byte cap) is the shared enabler for aesthetic RC1/RC2/RC6 (and the existing `safeFetchImage` is S3-only and cannot be reused as-is).
- The `progress` fixes need BOTH a worker-side "done" re-publish AND a client-side finalize-in-the-video_ready-branch (the client closes the SSE stream before `__finished__` arrives).agentId: a060b947859aec58d (use SendMessage with to: 'a060b947859aec58d' to continue this agent)
<usage>subagent_tokens: 171704
tool_uses: 15
duration_ms: 529221</usage>